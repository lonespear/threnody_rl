# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtual environments — leading slash anchors to repo root so we don't
# accidentally exclude `threnody_rl/env/` (the game-engine package).
/.venv/
/venv/
/env/
/ENV/

# Test / coverage
.pytest_cache/
.coverage
htmlcov/
.tox/
.mypy_cache/

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Training artifacts — too large for git, transfer separately
checkpoints/
logs/
replays/
training_out*.txt
runs/

# Build / dist
*.egg-info/
build/
dist/
